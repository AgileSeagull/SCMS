# Database (MongoDB)
# Local Docker: use directConnection=true so the driver doesn't try to resolve the container hostname
# Local: mongodb://localhost:27017/libraryflow?directConnection=true
# Atlas:  mongodb+srv://username:password@cluster.mongodb.net/libraryflow
DATABASE_URL="mongodb://localhost:27017/libraryflow?directConnection=true"

# JWT
JWT_SECRET="your-super-secret-jwt-key-here"

# Server
PORT=5000
NODE_ENV=development
